name: Vercel Auto-Deploy
on:
  push:
    branches:
      - main  # This triggers the update whenever you push to main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install and Build
        run: |
          npm install
          npm run build